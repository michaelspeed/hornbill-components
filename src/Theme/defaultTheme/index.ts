import {
    colors,
    elevations,
    fills,
    palette,
    scales
} from './foundational-styles'
import {headings, text, fontFamilies, paragraph} from "./typography";

export default {
    colors,
    elevations,
    fills,
    palette,
    scales,

    typography: {
        headings,
        text,
        fontFamilies,
        paragraph
    }
}
